from . import ... : import from same directory

intro
	install module : pip install django
	create project : django-admin startproject mysite
	run server : python manage.py runserver, http://127.0.0.1:8000/
	create app : python manage.py startapp webapp

project structure
	mysite : project container
		manage.py
		mysite : project app
			settings.py
			urls.py
			wsgi.py
			__init__.py
		personal : custom app
			static/personal : enable relative reference
			templates/personal
		app1
		app2
		...

steps
	app
		create : python manage.py startapp ...
		register
			mysite/mysite/settings.py, INSTALLED_APPS
			mysite/mysite/urls.py, urlpatterns
		code stuff
	model
		python manage.py migrate
		register
			mysite/blog/admin.py
			from .models import Post
			admin.site.register(Post)

view
	HttpResponse
		from django.http import HttpResponse
		def index(request):
			return HttpResponse("<h2>Hello Django!!</h2>")
	render
		from django.shortcuts import render
		def home(request):
			return render(request, 'personal/home.html')		

static
	{% load staticfiles %}
	<link href="{% static 'personal/css/bootstrap.min.css' %}" rel="stylesheet">

manage.py
	project management utility
	python manage.py ...
	runserver : starts server
	startapp ... : create app
	migration : sycronize model/db of project/app
		migrate : commit update
		makemigration : prepare update
		sqlmigrate : simulate update
	createsuperuser : for admin

model
	database related, ORM?

Hosting
	https://www.digitalocean.com/community/tutorials/how-to-serve-django-applications-with-apache-and-mod_wsgi-on-ubuntu-16-04
	Default setup (same as Flask)
		Default Python3 VirtualEnvironment 
	Django (in venv)
		~/myproject/myproject/settings.py
			add host, ALLOWED_HOSTS
			add static path, STATIC_ROOR
		./manage.py makemigrations
		./manage.py migrate
		./manage.py createsuperuser
		./manage.py collectstatic			
	Apache2
		/etc/apache2/sites-available/DjangoApp.conf
			add app info
			a2ensite DjangoApp
		~/myproject/myproject/wsgi.py
			add virtualenviroment part
	DB permissions (mysql?)