pip install flask

intro
	from flask import Flask
	app = Flask(__name__)
	...
	routed functions
	...
	if __name__ == '__main__':
		app.run(debug=True)

routing : link url to a function
	@app.route('/author')
	def author():
    	return '<h2>Blitz love Python!</h2>'

passing variables : url with variable
	@app.route('/post/<int:post_id>')
	def post(post_id):
		return "This is the contend of post {}".format(post_id)

HTTP methods : post get
	from flask import Flask, request
	@app.route('/methods', methods=['GET', 'POST'])
	def methods():
		if request.method == 'POST':
			return "Your request method is POST"
		else:
			return '''Your request method is GET
			<form action='/methods' method='POST'>
				Press OK!<br>
				<input type='submit' value='OK'/>
			</form>'''

HTML files
	code
		from flask import Flask, render_template
		@app.route('/profile/<name>')
		def profile(name):
			return render_template("profile.html", name=name)
	templates/profile.html
		<!DOCTYPE html>
		<title>{{name}}'s home</title>
		<link rel="stylesheet" type="text/css" href="{{url_for('static', filename='style.css')}}">
		<h1>{{name}}</h1>
		<h2>Welcome back!</h2>
	static/style.css
		h2 {color: deepskyblue;}
	HTML scripting
		{{ ... }} : variable passed from python to html
		{% ... %} : Python code in html
			{% if ... %}
			...html...
			{% else %}
			{% endif %}
			{% for ... %}
			...html...
			{% endfor %}

Hosting
	Tools
		PuTTY, Win-SCP
		bootstrap, jQuery
	Note
		File encoding : utf-8
		Win-SCP permission
			Advanced-Environment-SCP/Shell-Shell sudo su -
		Code change server auto reload
			app.config.update(TEMPLATES_AUTO_RELOAD = True)
		Routing
			End URL with '/' : accepts URL request both with or without ending '/'
			Multi routing : can route multiple URL to same function
	Default
		sudo apt-get update
		sudo apt-get upgrade
		sudo apt-get install apache2 mysql-client mysql-server
	Python3
		sudo apt-get install python3 python3-pip libapache2-mod-wsgi-py3
		sudo ln -s /usr/bin/python3 /usr/bin/python
		sudo a2enmod wsgi
	Flask-local : Flask in virtual environment at localhost
		make project directory /var/www/FlaskApp/FlaskApp
			FlaskApp static templates __init__.py
		sudo pip3 install --upgrade pip
		sudo pip3 install virtualenv
		sudo virtualenv venv
		sudo chown -R ubuntu:ubuntu venv
		source venv/bin/activate
		pip3 install Flask
		python __init__.py
	Flask-web : when Flask-local works
		deactivate
		sudo nano /etc/apache2/sites-available/FlaskApp.conf
		sudo nano /var/www/FlaskApp/flaskapp.wsgi
		sudo service apache2 restart
		check public ip on browser
